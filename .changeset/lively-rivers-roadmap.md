---
"@varlabs/anchor": minor
---

Add `anchor rm <name>` to delete a checklist regardless of completion state, `anchor lift --dry-run` to preview deletions, non-interactive checklist creation via `anchor set --name --items [--description]`, per-command `anchor <command> --help`, and extend `anchor validate` to check `.anchor/config.json`.
