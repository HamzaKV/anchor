## What & why

## Checklist

- [ ] `npm run lint`, `npm run typecheck`, `npm run build`, `npm test` pass locally
- [ ] Added/updated tests for the change
- [ ] Added a changeset (`npm run changeset`) if this affects published behavior
- [ ] Updated README.md if user-facing behavior changed
